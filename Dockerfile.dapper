FROM golang:1.8

ENV		DAPPER_SOURCE ${GOPATH}/src/github.com/rusenask/jenkins-whr-demo
ENV		DAPPER_OUTPUT .
ENV		DAPPER_DOCKER_SOCKET true
ENV		DAPPER_ENV VERSION JOBDATE
WORKDIR		${DAPPER_SOURCE}
ENTRYPOINT	["make"]